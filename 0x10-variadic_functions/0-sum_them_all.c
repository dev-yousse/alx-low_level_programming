#include "variadic_functions.h"
/**
 * sum_them_all - sum all parameter passed to the function
 * @n: number of argument to be passed to be passed to sun
 * Return: sum
 */

int sum_them_all(const unsigned int n, ...)

	{
		usigned int sum = 0;
		usigned int i;

		va_list holder;

                va_start (holder, n);

                 for (i = 0; i < n; i++ )
			 {
			 if (n = 0)
			 { return (0);
			 }
	                  else 

	}
